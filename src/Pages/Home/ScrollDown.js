import React from "react";
import scroll from "../../assets/Scroll/scroll.gif";

const ScrollDown = () => {
  return (
    <div>
      <img src={scroll} alt="" width={30} />
    </div>
  );
};

export default ScrollDown;
